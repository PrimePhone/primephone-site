name: Sync Suppliers to Supabase

on:
  schedule:
    # Щодня о 8:00 за київським часом (6:00 UTC)
    - cron: '0 6 * * *'
  # Або вручну через GitHub → Actions → Run workflow
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run sync script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
       run: python .github/workflows/sync_suppliers.py
